import React from 'react';

import './loader.scss';

const Loader = () => (
  <div className="loader">
    <div className="text">
      Chargement des données...
    </div>
  </div>
);

export default Loader;
